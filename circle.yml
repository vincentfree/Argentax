machine:
  java:
    version: oraclejdk8
deployment:
  staging:
    branch: master
    heroku:
      appname: argentax
test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/test_report/
    - find . -type f -regex ".*/target/failsafe-reports/" -exec cp {} $CIRCLE_TEST_REPORTS/test_report/ \;